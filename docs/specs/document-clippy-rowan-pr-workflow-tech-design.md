---
status: draft
task_id: e9c06d01-722e-4ad7-af3e-523464a42638
product_spec: n/a
shipped_pr: null
shipped_date: null
---

# Document clippy in Rowan PR workflow

## Links

- Product spec: n/a — workflow/process improvement task
- Tech design: `docs/specs/document-clippy-rowan-pr-workflow-tech-design.md`
- Task: `e9c06d01-722e-4ad7-af3e-523464a42638`
- Tasks API record: `http://localhost:4001/api/v1/tasks/e9c06d01-722e-4ad7-af3e-523464a42638`

## Repositories

- Primary repo: `Stoffer-Industries/sindustries`
- Branch: `task-e9c06d01-document-clippy-rowan-pr-workflow`
- Worktree: `~/workspaces/rowan/sindustries`
- Expected `.openclaw` follow-up: none — this is a docs-only change inside the repo.

## Scope

Update Rowan's code workflow and PR guidance so Rust workflow PRs (specifically `agents/workflows/feature-task/`) include clippy evidence before review. No code changes — documentation only.

## Shared scope note

This task overlaps with the other three clippy tasks in this batch:

- `cbe3333a` — Add feature-task clippy CI gate (CI wiring).
- `1a0fc7df` — Clean feature-task clippy baseline (code fixes).
- `55c98158` — Enforce clippy evidence in feature-task lobster (runtime enforcement).

This task is the **docs** layer of the same effort. All four tasks should land together (or in a deliberate order: baseline → CI gate → docs → lobster enforcement) so the docs describe real, enforced behavior rather than aspirational guidance.

## Ownership boundary

- The workflow doc lives at `agents/workflows/feature-task/WORKFLOW.md` (and any related skill files). It is repo-local — no `.openclaw` boundary. Quinn does not need to intervene.
- The PR-opening guidance/template is the existing `agents/agents/PR_OPENING_GUIDANCE.md` (or equivalent). We edit, not replace.

## Implementation plan

File/module scope:

- `agents/workflows/feature-task/WORKFLOW.md` — add a new section "Rust quality gates" stating that `cargo test` and `cargo clippy --manifest-path agents/workflows/feature-task/Cargo.toml --all-targets -- -D warnings` must pass locally before a PR is opened or marked ready. Reference the exact commands verbatim.
- `agents/agents/PR_OPENING_GUIDANCE.md` (or current PR template path) — add a validation block:
  ```
  ## Validation
  - [ ] `cargo test --manifest-path agents/workflows/feature-task/Cargo.toml` (Rust changes only)
  - [ ] `cargo clippy --manifest-path agents/workflows/feature-task/Cargo.toml --all-targets -- -D warnings` (Rust changes only)
  ```
  The checkboxes are intentionally conditional — the workflow explicitly states they are skipped for non-Rust/content-only PRs.
- `agents/workflows/feature-task/SKILL.md` (if it exists) — cross-reference the same commands so anyone reading the skill knows where the gate is documented.
- `CONTRIBUTING.md` — short note in the "Rust workflow" section pointing at the new guidance. One paragraph.

## Data model / API contract

- None.

## Workflow / cron / skill changes

- The skill files `agents/skills/dev/pr-open/SKILL.md` and `agents/skills/dev/pr-review/SKILL.md` should be cross-checked for any template that needs the new validation block. Update only if a template currently emits the PR body; do not introduce a new template here.

## Test plan (AC verification matrix)

| AC | Verification |
|---|---|
| AC1 — workflow docs require `cargo test` + `cargo clippy ... -D warnings` for Rust workflow changes before PR open/ready | `WORKFLOW.md` diff contains both commands verbatim. Grep test in CI: `rg "cargo clippy --manifest-path agents/workflows/feature-task/Cargo.toml --all-targets -- -D warnings" agents/workflows/feature-task/WORKFLOW.md` returns ≥1 match. |
| AC2 — PR-opening guidance/template includes a validation section with both commands when applicable | `PR_OPENING_GUIDANCE.md` diff contains the new validation block. Grep test: `rg "cargo clippy" agents/agents/PR_OPENING_GUIDANCE.md` returns ≥1 match. |
| AC3 — guidance explains clippy failures must be fixed before review unless explicitly accepted as a temporary exception | `WORKFLOW.md` contains the sentence (or close equivalent) "clippy failures must be fixed before review unless the PR explicitly documents an accepted temporary exception". |
| AC4 — new guidance does not require clippy for non-Rust/content-only PRs | `WORKFLOW.md` explicitly scopes the requirement to Rust workflow changes; the PR template checkboxes are tagged "(Rust changes only)". |

User-visible ACs: none. This is a docs/process change. E2E not applicable. Verification is at the docs/CI-grep layer.

## Open questions and risks

- **Cross-link drift**: if the lobster enforcement task lands first, the docs should match the exact error message. Order the tasks: baseline → CI gate → docs → lobster.
- **PR template vs PR body**: the PR template lives in `agents/agents/`. If PR bodies are generated by a Lobster pipeline, that generator must also emit the new validation block. Cross-check with the pr-open skill during implementation.

## Linked tasks

- `cbe3333a` — Add feature-task clippy CI gate
- `1a0fc7df` — Clean feature-task clippy baseline
- `55c98158` — Enforce clippy evidence in feature-task lobster
{
  "$schema": "https://raw.githubusercontent.com/oxc-project/oxc/main/crates/oxc_linter/src/schemas/oxlintrc.schema.json",
  "categories": {
    "correctness": "error"
  },
  "rules": {
    "no-unused-vars": "warn",
    "shadcn/no-restyle": [
      "warn",
      {
        "allow": ["layout"],
        "contracts": []
      }
    ],
    "shadcn/no-arbitrary-values": "error",
    "shadcn/no-raw-colors": "error",
    "shadcn/no-inline-styles": "warn",
    "shadcn/no-unknown-classes": "warn",
    "shadcn/require-static-classes": "warn"
  },
  "jsPlugins": [
    "@shadcn/lint"
  ],
  "settings": {
    "shadcn": {
      "ui": ["@sindustries/ui"],
      "_comment": "ACTIVE install — task 7986eb43 slice 5 baseline. Slice 4 moved the settings key from `shadcn-lint` (dormant-install placeholder) to `shadcn` (the key @shadcn/lint 0.1.0 actually reads) and registered all six shadcn rules at warn so the baseline triage could capture offences without blocking CI. Slice 5 promotes `no-raw-colors` and `no-arbitrary-values` to error (zero offences after the slice-5 fix on `transition-[border-color,box-shadow,transform]` in `packages/ui/src/react/index.jsx`). The other four rules (`no-restyle` 41, `no-inline-styles` 23, `no-unknown-classes` 685, `require-static-classes` 3) stay at warn with a documented Accepted baseline in `docs/systems/design-system.md` \"Design-system linting\" section; they promote to error after the kit CSS retirement task collapses the BEM/legacy class emissions and the per-app kit CSS contracts."
    }
  }
}

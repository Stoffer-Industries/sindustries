{
  "name": "@sindustries/ash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ash QA-verifier agent: utility module (AC extraction, evidence stripping, PR fetch). Ash's reasoning loop lives in the agent identity at ~/.openclaw/workspace/agents/ash/ and consumes these utilities through its own tool calls.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.1",
    "typescript": "^5.8.2",
    "vitest": "^4.1.8"
  }
}

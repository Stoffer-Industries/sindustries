# CLIPPY_NOTES — feature-task baseline cleanup

This file documents the warning inventory captured during the
`cargo clippy --manifest-path agents/workflows/feature-task/Cargo.toml --all-targets -- -D warnings`
baseline cleanup for task `1a0fc7df` (Clean feature-task clippy baseline).

It exists to satisfy AC3 (findings are either fixed or deliberately scoped with
documented rationale) and to track which fixes are deferred vs. addressed in
which commits.

## Source

Inventory captured at task `1a0fc7df` HEAD (`53f7b4a docs(specs): add tech design for feature-task-clippy-baseline-cleanup`).

Run command:

```
cargo clippy \
  --manifest-path agents/workflows/feature-task/Cargo.toml \
  --all-targets \
  -- -D warnings
```

Result: **23 warnings generated by the test target, 14 warnings generated by the bin
target (12 duplicates between them).** Clippy exits zero today (no `-D warnings`),
but fails under AC1's gate command.

## Note on `tagged_path_values`

`tagged_path_values` is referenced only in this cleanup's tech-design doc and in
the task description (AC2 example). It does **not** appear in any Rust source
file in `agents/workflows/feature-task/src/` — it has already been removed by
earlier work, so AC2 reduces to "verify nothing else newly-stranded-by-PR-#259
remains" and "explicitly remove or `#[allow(...)]` the two never-used helpers
that clippy now flags". This file documents both.

## Inventory (deduped by warning text)

### Addressed in this PR

| Warning | Where | Action |
|---|---|---|
| `useless use of format!` (8 occurrences) | `src/main.rs` lines 5238/5278/5324/5370/5439/5491/5537/6395 (test fixtures) | Replace `format!("…").to_string()` with the literal `…to_string()`. |
| `redundant closure` (2 occurrences) | `src/analytics.rs` lines 289/293 | Replace `|s| chrono_like_parse_to_unix(s)` with `chrono_like_parse_to_unix`. |
| `casting to the same type is unnecessary (i64 -> i64)` (6 occurrences) | `src/analytics.rs` lines 330/331/332/455/462 | Drop the redundant `as i64` casts. |
| `length comparison to zero` (1 occurrence) | `src/main.rs` line 3292 | Replace `vec.len() > 0` with `!vec.is_empty()`. |
| `found call to str::trim_start before str::split_whitespace` (1 occurrence) | `src/main.rs` line 3162 | Drop the redundant `trim_start()` call. |
| `calls to push immediately after creation` (1 occurrence) | `src/main.rs` line 6512 | Replace `let mut observed: Vec<String> = Vec::new(); observed.push(...);` with `vec![...]`. |
| `this expression creates a reference which is immediately dereferenced` (2 occurrences) | `src/main.rs` lines 1804/2032 | Drop the `&` prefix. |

### Annotated with `#[allow(...)]` + rationale (deferred — needs design judgment)

| Warning | Where | Rationale |
|---|---|---|
| `function spec_resync_signal_present is never used` | `src/main.rs` line 2872 | Test-only helper used by tests under `#[cfg(test)]`. Clippy's bin target can't see those calls. Allow with explicit reason so the gate can be enforced. |
| `function implementer_active_pr_urls_with is never used` | `src/main.rs` line 3195 | Same situation as above. Test-only helper referenced from the test module. Allow with explicit reason. |
| `field assignment outside of initializer for an instance created with Default::default()` (3 occurrences) | `src/main.rs` lines 266/5116/5335 | Used to build `LobsterState::default()` then attach it to an `Envelope { task: … }`. Refactoring to struct-update syntax would force a parallel default re-export for each `LobsterState` field. Allow with rationale; revisit only if a downstream change simplifies the field set. |

## Validation

After applying the addressed fixes above, the local gate command must exit
zero. CI will run the same command and report green (AC1).

`cargo test --manifest-path agents/workflows/feature-task/Cargo.toml`
must remain green (AC4). Both targets are exercised in CI today.

## Dead-code stranded-by-PR-#259 audit

`rg "tagged_path_values" agents/workflows/feature-task/` → zero hits (the symbol
is gone, no `#[allow(...)]` needed for it).

The two `never used` functions flagged by clippy (`spec_resync_signal_present`,
`implementer_active_pr_urls_with`) are test-only helpers reached from
`#[cfg(test)]` modules. They're annotated with `#[allow(dead_code, reason = "…")]`
in this PR. No other dead-code warnings appear.

## Open follow-ups

None for AC1/AC2/AC3/AC4 of task `1a0fc7df`. Linked follow-on tasks
(`cbe3333a`, `e9c06d01`, `55c98158`) build the clippy gate on top of this
clean baseline.